import {RootStackParamList} from '../typescript/types';
// Stacks paths
const bottomTabsStackPath = '/bottom-tabs-stack';

// Screen paths
const usersPath: keyof RootStackParamList = '/users';
const signUpPath: keyof RootStackParamList = '/sign-up';
const signUpStackPath: keyof RootStackParamList = '/sign-up-stack';
const usersStackPath: keyof RootStackParamList = '/users-stack';

export {
  bottomTabsStackPath,
  usersPath,
  signUpPath,
  usersStackPath,
  signUpStackPath
};
